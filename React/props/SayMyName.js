function SayMyName(props){

    return (
    <div>
        <p>Fala  ai {props.nome}, suave</p>
    </div>
    )
}
export default SayMyName;

// Aprendi a criar argumentos e chama-los na página principal!

// props = objeto com dados do componente

// (props) = recebe esses dados

// props.nome = acessa o valor

// { nome } = desestruturação (forma melhor)
